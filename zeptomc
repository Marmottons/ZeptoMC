#!/bin/bash

# Wrapper pour lancer zeptomc depuis le répertoire du projet
# Utilise le code source directement (dev mode)

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="$SCRIPT_DIR/src:$PYTHONPATH"

python -m zeptomc "$@"
